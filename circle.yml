deployment:
  production:
    branch: master
    commands:
      - build/prod.sh
  development:
    branch: feature/firebase
    commands:
      - build/dev.sh
test:
  override:
    - echo "skipping tests for now"
machine:
  node:
    version: 7.0.0
