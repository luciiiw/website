deployment:
  development:
    branch: feature/firebase
    commands:
      - build/dev.sh
  production:
    branch: master
    commands:
      - build/prod.sh
test:
  override:
    - echo "skipping tests for now"
