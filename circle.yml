deployment:
  production:
    branch: master
    commands:
      - npm install -g firebase-tools
      - sudo apt-get install ruby-sass 
      - sass public/site.scss public/site.css
      - firebase deploy --token=$FIREBASE_TOKEN --non-interactive
